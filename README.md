
  # Chunkify

  This is a code bundle for Chunkify. The original project is available at https://www.figma.com/design/oe9Q698vezCEtbu267UtH9/Chunkify.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  